# libasio

@@ TODO: add 1-2 paragrpah summary (see libevent, libthrift).

A cross-platform C++ library for network and low-level I/O programming that
provides developers with a consistent asynchronous model using a modern C++
approach.

`build2` package for the standalone version of the Asio library.

@@ TODO: add usage (see libevent, libthrift).

@@ Should we mention `-D_WIN32_WINNT=0x0601` (Windows 7 or later)?
